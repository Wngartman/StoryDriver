# StoryDriver domain icons

These local, current-color SVGs cover StoryDriver-specific concepts that do not have a single conventional UI symbol: story thread, director note, scene frame, continuity, and narration waveform.

Command controls continue to use the bundled Lucide set so familiar actions remain recognizable. Keep accessible text or an `aria-label` on every interactive control that uses these assets.
