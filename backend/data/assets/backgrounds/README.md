# StoryDriver workspace backgrounds

Place local `.png`, `.jpg`, `.jpeg`, or `.webp` files here, then choose **Refresh Backgrounds** in StoryDriver Settings.

Files remain local under `D:\StoryDriver`. Images larger than 25 MB, malformed images, and images beyond the safe dimension limits are ignored or rejected.
